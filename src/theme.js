import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
      background: {
        default: "#f5f7fb"
      },
      primary: {
        main: '#467fcf',
        contrastText: '#9aa0ac'
      }
    }
});

export default theme;
